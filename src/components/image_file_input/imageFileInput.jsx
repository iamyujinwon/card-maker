import React from 'react';
import styles from './imageFileInput.module.css';

const ImageFileInput = (props) => {
  return <button className={styles.button}>Upload Image</button>;
};

export default ImageFileInput;
